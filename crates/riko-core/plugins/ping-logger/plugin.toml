[plugin]
name = "ping-logger"
version = "1.0.0"
description = "Logs latency to playvortex.io every 5 seconds during a session to ~/.local/share/riko/ping.log"
type = "binary"
platforms = ["linux"]

[binary]
entrypoint = "ping-logger.sh"
run_after_launch = true
delay_secs = 10
